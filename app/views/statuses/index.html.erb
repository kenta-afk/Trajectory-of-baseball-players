<!DOCTYPE html>
<html>
<head>
<%= stylesheet_link_tag 'statuses_index', :media => "all" %>
</head>
<body>
  <h1 id="group-name"><%= @group.name %></h1>

  <p>Your group's UUID is: <span id="group-uuid"><%= @group.uuid %></span></p>

  <h2>Members</h2>
  <ul id="members-list">
    <% @members.each do |group_user| %>
      <li class="member-name">
        <%= group_user.user.name %>
        <!-- 各メンバーのステータスを新規作成するためのリンクを追加します -->
        <%= link_to 'New Status', new_group_status_path(group_id: group_user.group.id, user_id: group_user.user.id) %>
      </li>
    <% end %>
  </ul>

  <a href="<%= groups_path %>" id="back-link">Back</a>
</body>
</html>
